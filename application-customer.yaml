server:
  port: 8887

spring:
  application:
    name: customer

feign:
  hystrix:
   enabled: false
   command:
    default:
     execution:
      isolation:
       thread:
        timeoutInMilliseconds: 5000
      timeout:
       enabled: false  
